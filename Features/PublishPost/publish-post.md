# Feature: publish-post

## Purpose

Keep a small application-feature smoke fixture aligned with the canonical `Features/<Feature>/` layout.

## Current State

- The feature root is `Features/PublishPost/`.
- Runtime code lives in `Features/PublishPost/src/`.
- Tests live in `Features/PublishPost/tests/`.
- The manifest, schemas, prompt notes, context manifest, and support declarations live directly under the feature root.

## Open Questions

- Whether this smoke fixture should be replaced by a better demo fixture in a later examples rebuild.

## Next Steps

- Keep this fixture minimal until examples and demos are rebuilt.
