# Feature: context-persistence

## Purpose

Keep a small application-feature smoke fixture aligned with the canonical `Features/<Feature>/` layout.

## Current State

- The feature root is `Features/ContextPersistence/`.
- Runtime code lives in `Features/ContextPersistence/src/`.
- Tests live in `Features/ContextPersistence/tests/`.
- The manifest, schemas, prompt notes, context manifest, and support declarations live directly under the feature root.

## Open Questions

- Whether this smoke fixture should be renamed or removed when examples and demos are rebuilt.

## Next Steps

- Keep this fixture minimal until examples and demos are rebuilt.
