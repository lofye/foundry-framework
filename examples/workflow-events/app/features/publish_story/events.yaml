version: 1
emit:
  - name: story.published
    schema:
      type: object
      additionalProperties: false
      required: [story_id]
      properties:
        story_id:
          type: string
subscribe:
  - story.review_requested
