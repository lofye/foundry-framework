version: 1
emit:
  - name: story.submitted
    schema:
      type: object
      additionalProperties: false
      required: [story_id, title]
      properties:
        story_id:
          type: string
        title:
          type: string
subscribe: []
